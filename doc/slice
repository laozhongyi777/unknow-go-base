切片slice
    其本身并不是数组，它指向底层的数组
    作为变长数组的替代方案，可以关联底层数组的局部或全部
    为引用类型
    可以直接创建或从底层获取数组生成
    使用len()获取元素的个数，cap()获取容量
    一般使用make()创建
    如果使用多个slice指向相同的底层数组，其中一个的值改变会影响全部

    make([]T, len, cap)
    其中cap可以省略，则和len的值表示相同
    len表示存数的元素个数，cap表示容量　