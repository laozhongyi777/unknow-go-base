数组Array

    定义数组的格式：var varname  [n]type   注意n为数组元素个数，必须为正书
    数组长度也是类型的一部分，因此具有不同长度的数组为不同类型
    注意区分指向数组的指针和指针数组
    数组在go中为值类型
    数组之间可以用==或者!=　进行比较，但是不可以使用大于或小于
    可以用new来创建数组，此方法返回一个指向数组的指针
    go支持多维数组
